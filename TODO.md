# TODO

- [x] Fix PelatihController@show monthlyAttendance query to be compatible with SQLite (remove DATE_FORMAT for sqlite; use strftime) while keeping behavior for other DBs.
- [x] Run quick test by hitting the route (or run php artisan test / minimal command) and ensure no SQL error.



